# Codex 项目协作通用模板（AGENTS.md）

> 用于统一约束 Codex 在不同项目中的行为。  
> 项目可在本模板基础上追加“项目特有规则”。

---

## 1. 语言与沟通（硬性）

- 所有沟通回复必须使用中文。
- 每次任务完成后的回复，必须以 **“许哥在吗？”** 开头。
- 先给结论，再给改动点和文件路径。
- 发现阻塞或不确定项，必须立即说明，不允许隐瞒或跳过。

---

## 2. UI 设计规范（硬性）

### 2.1 必须使用 UI/UX PRO MAX skills

- 所有页面 UI 相关设计与实现必须调用 `ui-ux-pro-max`。
- 禁止用普通“平庸模板化”UI 直接交付。

### 2.2 禁止使用 emoji

- 代码、文案、按钮、标签、提示中均禁止使用 emoji。
- 使用文字或图标表达语义。

### 2.3 配色限制

- 禁止使用紫色系配色（如 `#800080`、`#A020F0`、`#EE82EE`、`#8A2BE2`、`#4B0082`）。

### 2.4 禁止深色配色

- 不允许以深色背景作为主视觉（包括纯黑、近黑、深灰大面积背景）。
- 非明确需求下，不实现暗黑模式风格。
---

## 3. 问题处理规范（硬性）

### 3.1 及时暴露问题

- 发现问题必须立即报告，不得隐藏。
- 不确定时先说明风险，再继续执行可确认部分。
- 构建错误、测试失败、关键警告不得跳过。

### 3.2 问题报告格式

```text
问题：[简要描述]
位置：[文件路径:行号]
影响：[造成的后果]
建议：[建议修复方案]
```

---

## 4. 代码与实现通用规则

- 优先复用现有目录结构与封装，不随意重构无关代码。
- 保持命名一致性：
  - 文件：`kebab-case`
  - 变量：`camelCase`
  - 常量：`UPPER_SNAKE_CASE`
- 不允许留下未处理的 `TODO` / `FIXME`。
- 不得引入硬编码密钥、Token、密码等敏感信息。

---

## 5. 任务完成检查清单

- [ ] 代码已落地，不是只给建议。
- [ ] 改动功能可运行，关键路径可验证。
- [ ] 未引入新的编译错误或类型错误。
- [ ] 相关测试/构建已执行（按项目实际要求）。
- [ ] 回复中包含关键改动文件路径与说明。

---

## 6. Git 自动提交规则（硬性）

### 6.1 自动提交触发条件

- 每次完成一个明确任务并通过基本验证后，必须执行一次 Git 提交。
- 如当前目录不是 Git 仓库，必须立即按“问题报告格式”说明。

### 6.2 提交范围

- 仅提交本次任务相关文件，不得混入无关改动。
- 若工作区存在他人或历史未完成改动，先说明再处理，禁止直接覆盖或回滚。

### 6.3 标准提交流程

```bash
git status
git add <本次任务相关文件...>
git commit -m "<type>: <简要中文说明>"
```

### 6.4 提交信息规范

- `type` 仅允许：`feat`、`fix`、`refactor`、`docs`、`test`、`chore`、`hotfix`
- 示例：`fix: 修复用户列表分页参数丢失问题`

### 6.5 异常处理

- 若提交失败（冲突、hook 失败、权限问题），必须立即报告并附错误摘要。
- 未经明确要求，不自动 `git push`。

---

## 7. 目录结构规范（多端分离，硬性）

- 必须按”后端 / 用户端 / 后台管理端”三端分离目录，不允许混放代码与任务文档。
- 每一端的目录名根据项目实际用途自动取名（如项目名-admin、项目名-api、项目名-app 等），不使用千篇一律的 `src`。
- 每一端目录内包含 `tasks/` 子目录存放任务文档，代码直接放在端目录下按框架约定组织。
- 新建项目默认按以下结构落地；老项目可逐步迁移，但新增内容必须遵循该结构。

```text
project-root/
  <项目名>-api/          # 后端（根据实际命名，如 xxx-server、xxx-api）
    tasks/               # 后端任务文档
    ...                  # 后端代码按框架约定组织

  <项目名>-app/          # 用户端（根据实际命名，如 xxx-h5、xxx-mp、xxx-app）
    tasks/               # 用户端任务文档
    ...                  # 用户端代码按框架约定组织

  <项目名>-admin/        # 后台管理端（根据实际命名，如 xxx-admin、xxx-manage）
    tasks/               # 后台任务文档
    ...                  # 后台代码按框架约定组织
```

- 命名原则：目录名应能直接看出”哪个项目的哪个端”，禁止使用 `src`、`code` 等无意义通用名。

---

## 8. 项目特有规则（按需追加）

在此补充项目专属约束，例如：

- 技术栈与目录约束
- 固定端口与启动脚本
- 接口返回结构
- 多语言文案同步
- 业务硬约束（登录方式、支付、鉴权、风控等）

---

## 9. 自动化测试 MCP 规则（硬性）

### 9.1 核心原则

- 任务完成后，必须主动调用已配置的 MCP 工具进行功能验证，不允许只写代码不验证。
- 根据当前任务涉及的端自动匹配对应的 MCP 工具，无需用户指定。
- 测试发现的问题必须当场修复，修复后再次测试验证，直到通过为止。

### 9.2 端与 MCP 工具匹配规则

| 涉及的端 | 使用的 MCP 工具 | 触发条件 |
|---------|---------------|---------|
| 后台管理页面（admin） | Playwright MCP | 任务涉及后台页面改动时，自动启动浏览器访问后台地址进行测试 |
| H5 / Web 页面 | Playwright MCP | 任务涉及 H5 或 Web 端页面改动时，自动启动浏览器访问对应地址进行测试 |
| 微信小程序 | weapp-dev-mcp | 用户明确给出"测试小程序"指令后，调用微信开发者工具 MCP 进行测试 |

### 9.3 后台管理 / H5 测试流程（Playwright MCP）

1. 确认后台或 H5 服务已启动（检查项目的 dev server 是否运行中）。
2. 调用 Playwright MCP 打开浏览器，访问对应页面地址。
3. 按任务改动范围执行验证：页面是否正常渲染、交互是否正常、接口是否正确返回。
4. 截图记录关键页面状态。
5. 若发现问题，立即按"问题报告格式"说明，然后修复代码并重新测试。

### 9.4 微信小程序测试流程（weapp-dev-mcp）

#### 重要前提：MCP 启动顺序

weapp-dev-mcp 在 Codex 启动时就会尝试连接 `ws://localhost:9420`。如果此时微信开发者工具的自动化端口未启动，MCP 会连接失败，整个会话都无法使用小程序测试工具。

**因此，正确的流程是：先启动自动化端口，再启动 Codex 会话。**

#### 9.4.1 用户说"测试小程序"时的处理流程

**第一步：检查 mp_* 工具是否可用**

先检查当前会话是否有 `mp_ensureConnection`、`mp_navigate`、`mp_screenshot` 等工具。

- **如果有** → 直接跳到"第三步：执行测试"。
- **如果没有** → 说明 MCP 未连接成功，执行第二步。

**第二步：引导用户启动自动化端口并重启会话**

1. 自动搜索小程序产物路径（不要硬编码，用以下命令搜索）：
   ```bash
   find <项目根目录> -path "*/dist/dev/mp-weixin" -type d 2>/dev/null | head -5
   ```
   - 搜索范围包括：`unpackage/`、`unpackage.bak*/`、用户端子目录等。
   - 如果找到多个，列出让用户确认。
   - 如果一个都找不到，提示用户先在 HBuilderX 中编译小程序到微信平台。

2. 找到路径后，自动启动自动化端口：
   ```bash
   /Applications/wechatwebdevtools.app/Contents/MacOS/cli auto \
     --project <找到的mp-weixin路径> --auto-port 9420
   ```

3. 启动成功后，告知用户：
   ```
   自动化端口已启动。请重新打开一个 Codex 会话，然后再次输入"测试小程序"即可。
   ```

**第三步：执行测试**

当 mp_* 工具可用时，执行以下测试：

1. 调用 `mp_ensureConnection` 连接微信开发者工具。
2. 调用 `mp_navigate` 跳转到任务涉及的页面。
3. 调用 `mp_screenshot` 截图查看页面状态。
4. 调用 `page_getElement` 检查关键元素是否存在。
5. 调用 `mp_callWx` 验证需要的微信 API 调用。
6. 调用 `mp_getLogs` 检查控制台是否有报错。
7. 若发现问题，立即修复代码并重复上述步骤验证。

### 9.5 测试报告格式

每次测试完成后，必须输出简要测试报告：

```text
测试端：[后台管理 / H5 / 微信小程序]
测试页面：[页面路径或 URL]
测试结果：[通过 / 未通过]
发现问题：[无 / 问题描述]
修复情况：[已修复并验证通过 / 待用户确认]
```

### 9.6 注意事项

- 不要硬编码项目地址或端口，根据项目实际配置自动识别。
- 小程序测试必须等待用户给出"测试小程序"指令。
- 搜索产物路径时禁止硬编码，必须用 `find` 命令在项目目录下递归搜索 `mp-weixin` 目录。
- 如果当前会话没有 mp_* 工具，不要反复尝试连接，直接引导用户启动端口后重开会话。
- 后台和 H5 测试可在任务完成后主动执行，无需等待用户指令。
- 测试过程中如遇到登录页面，提示用户手动登录后继续测试。

---

## 10. 用户端编译禁令（最高优先级，硬性）

- **严禁执行任何用户端编译/构建命令**，包括但不限于：
  - `npm run dev:*`
  - `npm run build:*`
  - `uni -p ...`
  - `vite build` / `vite dev`
- 严禁生成、覆盖、同步用户端构建产物目录（如 `dist/`、`unpackage/dist/`）。
- 仅允许修改源码与配置，不允许以“排查问题”为由触发用户端编译。
- 若确需验证，必须由用户本人在本地自行编译，Codex 只提供命令与检查项，不代执行。
