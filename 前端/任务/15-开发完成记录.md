# 模块15：前端开发完成记录

## 完成时间
- 2026-02-10

## 开发范围
- 已基于 React 18 + TypeScript + Vite 在 `前端/app` 完成用户端工程搭建。
- 已按模块01-14任务文档完成页面、组件、路由、状态管理、API封装与基础交互。
- 已完成“前后端联调与真数据接入”补充，详情页、编辑页、个人页从静态骨架切换为真实接口数据流。

## 已完成模块
1. 首页与导航布局
2. 用户认证系统
3. 论坛讨论模块
4. 代码分享模块
5. 技术问答模块
6. 项目展示模块
7. 技术博客模块
8. 个人中心模块
9. 评论互动模块
10. 消息通知模块
11. 搜索系统模块
12. 标签分类模块
13. 排行榜与积分模块
14. 通用组件与工具库

## 联调补充
- 详情页改造：论坛、代码、问答、项目、博客、系列页接入真实接口。
- 编辑页改造：帖子、代码、问题、项目、博客接入“先拉取详情再更新”。
- 个人页改造：粉丝、关注、收藏、设置接入真实接口并支持保存。
- 交互补充：详情页评论列表与发表评论接入真实接口。
- API修正：评论查询参数改为 `targetType/targetId`；问答新增 `answers` 接口；代码新增 `favorite` 接口。

## 关键产物
- 路由与布局：`src/router/index.tsx`、`src/layouts/*`
- 通用组件：`src/components/ui/*`
- 业务组件：`src/components/shared/*`
- API封装：`src/api/*`
- Hooks与工具库：`src/hooks/*`、`src/utils/*`
- 页面模块：`src/pages/*`

## 验证结果
- `cd 前端/app && npm run lint` 通过
- `cd 前端/app && npm run build` 通过

## 说明
- 全站采用科技蓝主题，已支持亮色/暗色模式切换。
- 已避免使用紫色配色与 emoji。

## 2026-02-13 增量修复（站点名与联动数据）
- 站点名称去硬编码：分享海报、站点状态默认值改为读取 `siteName`，避免出现旧项目名称。
- 博客分类改造：`BlogList` 分类导航改为由真实博客数据动态生成，不再使用固定分类常量。
- 论坛标签改造：论坛列表标签筛选改为仅使用真实热门标签接口结果，不再混入本地默认标签。
- 代码广场分类改造：组件列表分类侧栏由真实列表数据动态生成，解决固定分类与真实内容脱节问题。

## 本轮验证
- `cd 前端/app && npm run -s build` 通过。

## 2026-02-13 增量修复（二）：字体系统升级
- 全站字体升级为 `Inter + Noto Sans SC + JetBrains Mono`，提升中文、英文、数字混排观感与一致性。
- 海报生成画布字体同步升级，避免海报文本仍使用旧字体。
- 代码预览 iframe 字体同步升级，保证组件预览区与页面主视觉一致。

## 本轮验证（二）
- `cd 前端/app && npm run -s build` 通过。

## 2026-02-13 增量修复（三）：去写死深扫（前端）
- 论坛发帖/编辑页标签来源改为仅使用真实热标签接口，不再合并本地默认标签：
  - `src/pages/Forum/CreatePost.tsx`
  - `src/pages/Forum/EditPost.tsx`
- 个人中心收藏夹摘要文案回退改为中性值“未命名收藏夹”，减少固定业务词带来的误导：
  - `src/pages/User/UserFavorites.tsx`
  - `src/pages/User/UserProfile.tsx`

## 本轮验证（三）
- `cd 前端/app && npm run -s build` 通过。

## 2026-02-14 增量修复（四）：站点配置实时刷新与残留写死清理
- 站点配置联动增强：
  - `siteStore` 增加缓存 TTL，避免一次加载后长期不更新；
  - 页面窗口重新获得焦点时强制刷新站点配置，后台改站点名后前端可自动同步。
- 残留示例写死清理：
  - 清理 `User/components/SettingsForm.tsx` 中旧示例默认值，避免出现历史项目文案。

## 本轮验证（四）
- `cd 前端/app && npm run -s build` 通过。

## 2026-02-14 增量修复（五）：用户端输入交互去 prompt
- 论坛发帖/编辑页“申请新板块”由 `window.prompt` 改为表单弹窗输入：
  - `src/pages/Forum/CreatePost.tsx`
  - `src/pages/Forum/EditPost.tsx`
- 评论区举报交互由 `window.prompt` 改为弹窗选择+说明输入：
  - `src/components/Comment/CommentSection.tsx`

## 本轮验证（五）
- `cd 前端/app && npm run -s build` 通过。
